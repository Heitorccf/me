---
import Layout from '../layouts/Layout.astro';
import { Image } from 'astro:assets';

import softPic from '../assets/soft.JPG'; 

const basePath = import.meta.env.BASE_URL;
---

<Layout title="Home | Heitor.dev">
  <div class="grid grid-cols-1 md:grid-cols-2 gap-8 md:min-h-screen">
    
    <div class="flex flex-col justify-center py-20 md:py-32 px-4 md:px-8 min-h-[80vh] md:min-h-[150vh]">
      <span class="text-blue-600 dark:text-blue-400 font-medium tracking-wider mb-4">
        DESENVOLVEDOR WEB
      </span>
      
      <h1 class="text-4xl md:text-6xl lg:text-7xl font-bold mb-6 text-gray-900 dark:text-white leading-tight">
        Transformando ideias em <br />
        <span class="bg-clip-text text-transparent bg-gradient-to-r from-blue-600 to-teal-400">
          realidade digital.
        </span>
      </h1>
      
      <p class="text-lg md:text-xl text-gray-600 dark:text-gray-400 mb-10 leading-relaxed max-w-xl">
        Especialista em Astro, Svelte e Tailwind CSS. Foco obsessivo em performance, acessibilidade e design que funciona. 
        Vamos construir algo rápido juntos.
      </p>

      <div class="flex flex-wrap gap-4">
        <a 
          href={`${basePath}/archive`} 
          class="px-8 py-4 bg-blue-600 hover:bg-blue-700 text-white font-bold rounded-lg transition-all transform hover:-translate-y-1 shadow-lg hover:shadow-blue-500/25"
        >
          Ver Projetos
        </a>
        
        <a 
          href={`${basePath}/about`} 
          class="px-8 py-4 bg-gray-100 dark:bg-gray-800 hover:bg-gray-200 dark:hover:bg-gray-700 text-gray-900 dark:text-white font-bold rounded-lg transition-all border border-transparent hover:border-gray-300 dark:hover:border-gray-600"
        >
          Sobre Mim
        </a>
      </div>
      
      <p class="mt-20 text-gray-500 text-sm">
        Role para baixo para ver a imagem da direita fixada...
        <br><br><br><br><br>
        Mais conteúdo aqui...
        <br><br><br><br><br>
        E mais um pouco...
      </p>
    </div>

    <div class="hidden md:flex md:sticky md:top-16 md:h-[calc(100vh-4rem)] items-center justify-center p-8 overflow-hidden">
      <div class="relative group w-full max-w-2xl">
        <div class="absolute -inset-2 bg-gradient-to-tr from-blue-600/20 to-teal-400/20 rounded-3xl blur-2xl opacity-0 group-hover:opacity-100 transition duration-700"></div>
        
        <Image 
          src={softPic} 
          alt="Imagem de destaque" 
          width={900} 
          height={600}
          quality="high"
          class="relative z-10 w-full h-auto object-cover rounded-3xl shadow-2xl shadow-black/10 dark:shadow-black/50 transition-transform duration-500 group-hover:scale-[1.02]"
          loading="eager" 
        />
      </div>
    </div>

  </div>
</Layout>